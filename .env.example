# Supabase (cloud sync + auth). See supabase/README.md for setup.
# Both vars are PUBLIC — anon key is JWT-signed, RLS protects data.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Anthropic (AI tag suggestions — server-side only, never exposed to client)
ANTHROPIC_API_KEY=
